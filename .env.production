# Production (Vercel): the browser calls our serverless proxy, NOT Anthropic directly.
# The real key is set server-side in Vercel as ANTHROPIC_API_KEY (no VITE_ prefix,
# so it is never exposed to the browser).
VITE_PROXY_URL=/api/messages
